---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: virtualbox
lint:
  flake8
platforms:
  - name: centos8
    box: centos/8
provisioner:
  name: ansible
verifier:
  name: ansible
